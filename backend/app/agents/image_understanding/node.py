from app.graph.state import IngestionState


async def image_understanding_node(state: IngestionState) -> dict:
    """Pass-through in Phase 1 - see logic.py for the Phase 2 activation plan."""
    return {}
